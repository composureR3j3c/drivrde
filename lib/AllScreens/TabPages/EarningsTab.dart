import 'package:flutter/material.dart';

class EarnigsTab extends StatelessWidget {
  const EarnigsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
